# Smooth front with the left jut restored

The user liked the distinctive left projection and asked to retain it while
viewing the new drawing beside the original island. This edit restores one
rounded tongue and recess on the left, retaining the broad smooth front.
It remains an unapproved shape study; no application asset or code changed.

`generation.json` and `prompt.txt` preserve the exact built-in image-generation
request, ordered inputs and raw output. The original artwork in the comparison
is not AI-redrawn. It is the previously retained composition of extracted
000/009/003/007 sprites from `../reference-master/`, including original wave
art, in diagnostic colors. It is a geometry reference, not evidence of the
original executable's palette. The selector also offers the earlier Cartoon
island and the preceding broad smooth draft to clarify either meaning of
"original island."

The generator enlarged the entire artwork despite the fixed-scale prompt.
The original raw is retained intact. For a fair side-by-side shape review,
the browser applies one uniform display scale of 0.8370423546302943 and
translation (116.22182340272792, 87.36109117013643), then uses the same crop
as each reference. No silhouette deformation or pixel repainting is used.
`comparison.json` records all source identities and this display mapping.

The mapping aligns 221 stable upper-edge samples to the previous draft with
0.85 raw-pixel RMS error; tip vertical differences are about 3-4 raw pixels.
It is suitable for shape review, not accepted runtime registration. Do not
silently promote this transform into the pack. The larger scenery bounds and
new shoreline-compatible wave art described in `../smooth-shore-v1/README.md`
remain required after human shape approval.

Source checks verified all seven comparison/generation bindings, RGBA canvas
and the saved prompt against the generation record. Browser inspection showed
the original comparison and successfully loaded the earlier Cartoon reference.
The page was returned to Original source artwork. These are source/display
checks, not application smoke or animation regression results.
