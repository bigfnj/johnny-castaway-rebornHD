# Foam visibility v2 native checkpoint

This retained run uses the frozen `../../native-v2` prepare/capture/launcher
helpers without changes. The four cases are no decoration, clovers, pumpkin
and tree at high tide/day/zero offset. Each package uses the same exact V5
holiday payloads. The comparison baseline is the earlier production island;
the candidate uses the shared ground master with visibility-masked foam.

All eight smoke captures passed before eight exact fresh-process repeats.
Actual base000 and final003/007/009 draws, selected PNG paths and canvas-limited
scene differences passed. Three copied-log/pixel controls failed by name and
unchanged positive inputs passed afterward. No compiler warnings occurred,
production inputs stayed unchanged, and the Xvfb container was removed.

Compared directly with the preceding shared-master candidate, exactly nine
wave PNG members changed;000 and2589 total payloads stayed byte-identical.
The operation masks foam visibility by inverse master alpha. It is not claimed
to be exact ground-over-foam composition. Clover-pad paint remains a separate
issue, and this checkpoint contains no later local ground extension. Inherited
master clipping and canvas-union losses remain documented in the export report.

Executed commands, from the repository root:

```text
python -B art/cartoon/shoreline-repair-v1/native-v2/prepare.py --baseline build/shoreline-repair-v1/selected-v1/baseline.zip --runtime-root art/cartoon/shoreline-repair-v1/foam-contact-v2/candidates/v2 --output build/shoreline-repair-v1/selected-foam-v2
python -B art/cartoon/shoreline-repair-v1/native-v2/run.py --baseline build/shoreline-repair-v1/selected-v1/baseline.zip --candidate build/shoreline-repair-v1/selected-foam-v2/candidate.zip --output build/shoreline-repair-v1/native-foam-v2
```

Use fresh output names for replay and reconstruct the pinned production
baseline after any later promotion. The source helpers and original evidence
stay immutable. This folder retains only small exact reports/logs and source
bindings; large PNG/PPM/ZIP/executable hashes are indexed separately. Static
technical checks do not establish all-phase animation or human approval.
