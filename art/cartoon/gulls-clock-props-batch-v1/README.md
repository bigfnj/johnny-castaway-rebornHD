# Gulls, clock and props: 71-drawing appearance review

Open [the review](http://127.0.0.1:8941/gulls-clock-props-batch-v1/review.html). Every new drawing has its exact original below it. Group links and the frame filter navigate the 71 entries.

This batch contains 26 gull poses, 17 pocket-watch pieces, three sleep symbols, 12 aircraft/rotor/parachute pieces, nine bottle poses and four water effects. It follows the user's 60-72-drawing default. Appearance approval is pending.

All artwork was generated with the built-in `image_gen.imagegen` tool. The complete prompt set is `generation/*/*-request.json`; unmodified returned PNGs and reference/output hash records are beside each prompt. `selected-versions.json` identifies selected corrections. `review-data.json` and `review-record.json` pin the displayed selections.

Read [source-notes.md](source-notes.md) for pose constraints, duplicate exclusions and source-palette limits. The originals come from the supplied-original reference archive, not earlier HD approximations.

Only lightweight image and gallery checks accompany this review. Native canvas fitting, multipart joins, sequence motion and full smoke/regression remain for bulk integration. Production remains at 63 PNGs; `assets/scrantic_data.zip` is unchanged at SHA256 `a87a1f52b85277d88129347654a508edf9ca1f82920a31e20b5b41216242f63d`.

To rebuild this new review after all selected outputs are present, run the toolbox Python on `build_review.py` from this directory. Preserve historical review and acceptance files when creating subsequent corrections.
