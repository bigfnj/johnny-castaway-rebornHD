# Fresh core review after PR 18

Reviewed the primary checkout at `024c9943a08ede16cfa24ed37aa768cf1f020b63`. This was a fresh read of all 37 files under `src`, including the engine implementation, public headers and static story, path and walking data. No new concrete blocker was found. Git confirms that this entire source tree is unchanged from the pre-low-tide baseline `da787d6`; unchanged code was still reviewed because the new art activates an existing rendering path.

`core-review.json` records the exact commit, baseline, source file inventory, Git blob IDs and SHA-256 hashes of both committed and checked-out bytes. Windows checkout line endings are recorded separately rather than treated as frozen-byte identity. The report does not replace the separate package, authoring, platform or native gate results.

| Reviewed path | Findings and coverage |
| --- | --- |
| `src/engine/art_style.c`, `graphics.c`, `island.c` and their headers | Read PNG selection and fallback, narrow registered dimensions, normal/flip/atop placement, clipping, sprite ownership, background replacement and wave restoration. Low-tide frames retain their original logical origins and ordinary 2x canvases. `islandSaveWaveBase` at line 62 collects every active family's phase dimensions and placement offsets, clips its snapshot to the background, and `islandDrawWave` at line 116 restores before repainting initialized families. The new low-tide PNGs activate this existing path; comparing raw draw-call counts to fallback stamping would be misleading. `islandRelease` and screen teardown release the snapshot. No new leak or placement-contract defect was found in these paths. |
| `src/engine/ads.c`, `ttm.c`, `story.c`, `walk.c`, `calcpath.c`, `bench.c` and associated data/headers | Read thread/slot ownership, scene stop and reset, borrowed resource data, tag dispatch, timers, story eligibility, path selection, walking bookmarks and termination. Unfinished graphics commands are reachable through TTM dispatch, not unused functions. Static table inspection did not establish original-executable behavioral parity or coverage of every story branch. |
| `src/engine/resource.c`, `uncompress.c`, `dump.c`, `utils.c` and headers | Read stream closure, allocation and borrowing contracts, bounded packed-pixel access, decoder exits, script inspection and dump output. Resource arrays remain process-owned; this is distinct from recurring scene-layer leaks. Existing malformed-input and dump-error limitations below remain. |
| `src/engine/jc_reborn.c`, `config.c`, `events.c`, `sound.c` and headers | Read startup, CLI dispatch, configuration persistence, event exit, bounded capture termination, audio buffer ownership and shutdown order. The existing common-audio startup race and malformed saved-day/seed parsing items remain; no delivery-specific failure was identified. |

The following observations are already recorded in `BACKLOG.md` and are not new findings: audio state initialization after worker startup and the zero-length null-source copy; configured-day overflow and unchecked parsing; direction-dependent first waiting pose timing; unfinished TTM commands and primitive clipping semantics; missing sample-ID mapping; malformed LZW and overlong TTM strings; incomplete TTM tag tables; resource-dump write/close failures; process-lifetime resource arrays; redundant decoder-result null checks; and persistent wave counters across scenes. Their existing evidence and original-comparison requirements still apply. This audit did not re-execute historical fault probes.

No measured optimization is proposed. In particular, the per-column mirrored sprite path is an implementation choice, not an established performance regression. The redundant decoder-result checks are already a documented cleanup opportunity without a claimed speed benefit.

This was a read-only source and Git-byte audit. It did not run new tests, sanitizers, fuzzing, original-executable comparisons or benchmarks, and it does not claim exhaustive runtime behavior from static reading. No runtime or art source was edited. The unrelated untracked `-e` file was left untouched; concurrent parent documentation edits were left in place.
