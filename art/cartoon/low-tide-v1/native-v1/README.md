# Low-tide native draft evidence

Current Cartoon production `4d8e573b79b7f0dffdd0fefda6f2fa0833534a1649aa1ff0d2d3bf4724a398c6` is compared with provisional static candidate V2 `f46e5cac5508b00cc7a675be4eaf843029cebfeabe80c849c8fa5f311813b92b`. The candidate preserves every one of the 2,598 prior ZIP members and adds only Cartoon BACKGRND001 (768 by 138) and002 (128 by 60). There are no wave, renderer or coordinate edits in this test.

The current-production baseline and candidate each passed no-decoration and clover smoke captures, followed by exact fresh-process repeats. Each case contains 61 actual native displays over 144 requested ticks, or 2,880 ms, and observes all twelve low-wave frames030 through041. These are logical timing requests, not a wall-clock performance measurement. Paired timing, native calls, Johnny and draw records match. Every displayed pixel outside the exclusive HD rectangles `[498,606,1266,744]` and `[300,656,428,716]` is unchanged. Damaged old-wave metadata, a wrong001 canvas and an outside pixel each trigger their named check; restored positives pass.

`images/prewave-current.png` and `images/prewave-candidate.png` are real native background surfaces captured immediately after BACKGRND002 draws, before any wave draw. They contain neither Johnny, clouds nor holiday decoration. They are not normal displayed scenes and were not assembled from separate pictures. The scratch hook records its executed location, writes the actual surface and leaves rendering untouched. Its three subsequent displayed frames match the unmodified observer's corresponding prefix exactly for both packages. The complete hooked driver is retained at `helpers/prewave-driver.c`, along with the original observer and its hashes.

`images/current-{none,clover}.png` and `images/candidate-{none,clover}.png` are complete actual final displays. The old low-wave assets remain unchanged and can stamp their opaque pixel-art sand and surf over the new beach. The static views do not establish completed low-wave styling or final artwork approval.

Every capture ran in the existing pinned Docker image with an isolated Xvfb, no network and a read-only repository mount. Host launchers use hidden subprocesses. No workstation display or original executable was tested. The driver selects daytime, low tide, seed11 and the specified holiday directly, bypassing calendar and cargo eligibility. Johnny016 is present at native299,243 in the normal displayed cases; no walking route is selected.

The baseline executable was freshly compiled from the recorded source checkout. Candidate full captures reuse those exact executable bytes. The pre-wave driver has its own fresh build record. `evidence.json` records every exact copy, original scratch path and SHA256. `readback.json` verifies copied and original bytes. Bulk PNG/PPM sequences, private ZIPs and executables remain in scratch; retained display reports bind every displayed decoded RGB hash and PNG hash. Source pins identify the historical checkout and helpers, not supplied-original execution.

To reproduce, restore the saved adapters to their original repository-relative scratch paths in a checkout matching the source pins. Recreate the candidate from its separate authoring recipe and verify its archive hash. Run the baseline launcher first, then the candidate launcher with explicit `--candidate` and `--candidate-sha256`, then `run_prewave.py` with the same arguments. Launch JSON files preserve the exact commands and image identity. Use new output folders or an isolated fresh checkout; the launchers refuse existing evidence. The preservation writer is one-time archival work and should not be rerun into this frozen folder.
