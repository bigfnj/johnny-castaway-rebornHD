# Cartoon sandcastle family drafts

The current [24-drawing review](../props-batch-v1/review.html) includes SANDCAST.BMP000-014. This family contains no Johnny pixels:000 is the complete castle,001-005 change its towers/walls,006-009 are sand effects, and010-014 are collapse/rubble states. These are character-action dependencies, so their fit against Johnny will be checked during integration.

The user approved the appearance of all fifteen selected drawings in the [combined prop review](../props-batch-v1/acceptance/appearance-v1.json). Final geometry/registration and native behavior remain pending. The supplied originals guide each frame's geometry; the approved island controls the warm sand palette. Generated000 serves as the shared style reference across the remaining fourteen built-in ImageGen calls. Exact requests, unchanged raw PNGs and actual output metadata are saved under `generation/`. The original diagnostic palette is not verified original-executable color.

The review selects v2 for009 (complete particle field with transparent margin) and010 (restored dark cartoon contours); all other slots use v1. Earlier outputs remain preserved. The generated architecture and proportions differ from the originals, especially the wider003 silhouette. These drafts do not establish original geometry parity or runtime fit; see each subgroup's observations before exporting.

[Preflight](preflight.md) and `reference/source.json` retain source identities and static script associations. Original007 has no unique-slot draw attribution in the existing static map; including its artwork does not prove a runtime scene uses it. Original canvases, offsets, scripted timing and draw order remain unchanged.

This is an art-build checkpoint. Final export registration, in-scene scale, contact with Johnny, collapse timing and particle behavior remain pending under the [bulk verification workflow](../../../docs/cartoon-art-build-workflow.md). No production package, catalog or engine changes are included.
