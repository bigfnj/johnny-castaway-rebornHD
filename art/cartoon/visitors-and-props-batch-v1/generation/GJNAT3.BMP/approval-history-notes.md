# Male visitor draft observations, 2026-09-20

This worker generated the 14 assigned upper-body drawings 000 through 011, 048 and 049. Suggested review versions are 008 v2 and v1 for every other assigned frame. Shared identity key 002 was generated first. Original per-frame nearest-neighbor references control the pose; the red/yellow source checker was interpreted as diagnostic skin dithering, so these drawings have bare tan upper bodies, not patterned shirts. No lower body was added.

008 v2 corrects the reversed image-left elbow bend in v1: the hand is now outside and above the lower inward elbow. 005 remains a fidelity caveat for review. Its v1 free hand extends below the waist cut, while the source hand ends close to that level. The preserved v2 correction folded the arm too far inward. Preserved v3 restored the extended reach but did not materially raise its endpoint. V1 is suggested over those two retries because it retains the clearer original extended-arm action; this does not declare the endpoint discrepancy fixed.

Every generated output was inspected. All 17 output versions are untouched byte-exact copies of the built-in tool's PNGs. A bounded readback verified every saved request, ordered reference and output SHA-256 against the files, and every output against its raw cache copy. All images are RGBA with alpha range 0 through 255. Every alpha-8 contour stays inside its canvas; selected 004 and 009 retain edge alpha 1, and 049 retains edge alpha 2. The 049 top and bottom meaningful margins are only 8 pixels, but its contour is not cut off. No alpha cleanup, pixel edits, runtime registration, native playback, export, package change or human appearance approval is claimed.
