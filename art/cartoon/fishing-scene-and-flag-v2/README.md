# Fishing colors and hanging flag

This targeted review follows the user's two comments on the 64-drawing batch: show MJFISH3.BMP 008-010 in their fishing scene before deciding their colors, and make GJBIPLAN.BMP 021 the longer flag from 022 hanging loosely from its upper connection.

[Open the review](review.html). The fish buttons select actual native engine captures, with an enlarged scene view and fish close-up. Switch between the current Cartoon environment with existing HD fish fallback and Original-mode source rendering. The diagnostic gallery original and the current Cartoon drawing remain alongside the capture. Cartoon fish colors have not been changed. Capture identity, selected frames, crop coordinates and limitations are retained in [scene-manifest.json](scene-manifest.json).

The native captures directly replay MJFISH.TTM setup tag 1 followed by action tag 29, using seed 11 and the day/high-tide island. All three selected poses retain their original scripted flips and positions, with Johnny grounded at the island edge. The smaller source fish use dark green/red colors rather than the brighter colors in 002-004; this does not establish a lighting or distance intent. The port's Original-mode palette is not calibrated to the historical Windows executable, and direct tag playback does not prove normal ADS story reachability. These limitations apply to the color comparison, not the saved flag correction.

The corrected flag uses a short upper connection and a free lower length, with transparent space beside the pole. [Its built-in image-generation request](generation/GJBIPLAN.BMP/021-v1-request.json), untouched [raw output](generation/GJBIPLAN.BMP/021-generated-v1.png) and [input/output record](generation/GJBIPLAN.BMP/021-record-v1.json) are preserved. The earlier flag and 022 reference remain visible in the review.

The [feedback record](feedback.json) retains the user's exact request and attached screenshots. No other drawing in the 64-drawing batch gains implied approval. This page is for appearance review; runtime fitting of Cartoon assets, full smoke tests and bulk integration remain deferred. The production archive is unchanged.
