# Revised018 native still review

This folder supports the user's request for longer018 proportions, lower shorts
and two feet reaching the island. It does not edit original captures or production
assets, and it does not grant human approval.

`smoke.py` retained the unnormalized v2 geometry inspection at
`build/standing018-proportions/native-review/v2`. That is one native display only.
Its exact executed helper hash is in its report. Do not rewrite that checkpoint.

`capture_review.py` replaces only018 in the accepted-color private baseline
`bdc62b0c34835196e6dfa6541ee54f9c72f9824a0c24a0beab4bee3a33393eaf`.
It keeps all2593 other member payloads exact and uses the existing observer binary
`203aebb8c15881a41108b2b4fb97f5f9c6d025aa595a678b4dd84906011f7563`.
The normalized input has its own new skin mask and color recipe; the earlier018
mask is not reused. Normalizedv2 SHA is
`21cf94cd90d369b20b6d3b8ef60b7cc2848f919ff828578320502aec9a17c55b`.

The selected run captures Front turn smoke, then full and fresh repeat. Actual
API/path/draw/wait/display transcripts must match the frozen baseline. Changes
are confined to the placed64x154018 canvas; all non018 displays remain exact.
This basic regression supports the still review. Later motion review still needs
Front waypoint arrival and Rear waypoint mirrored arrival after proportion approval.

`build_still.py` renders no new artwork. It copies the three first-display native
PNGs: supplied original018 on the Cartoon island, earlier Cartoon018, and revised
Cartoon018. All have the same draw `(478,216)` and scene crops. Original colors
use the port diagnostic palette and are explicitly not an original-executable
appearance claim. The unchanged contact018 `check_review.py` checks local and
served exact pixels, image hashes and full-scene expansion.

Run capture with explicit `--runtime`, `--runtime-sha256`, `--color-record` and a
new `--output`. Build the still page with that output as `--candidate` and a fresh
`--output`. Both refuse existing output directories. The native helper uses the
pinned Docker image and Xvfb, with source mounted read-only. No workstation
foreground window is opened. Preserve historical pages and evidence; reconstruction
requires the pinned baseline/observer and the linked exporter/color inputs.
