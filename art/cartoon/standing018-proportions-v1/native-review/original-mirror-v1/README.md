# Original018 at the mirrored arrival

This bounded diagnostic answers whether the smaller-foot gap also occurs with
the supplied original pose at the exact rear waypoint arrival. It reuses the
unchanged native observer and the frozen `original_johnny` archive
`d7797181cf2e30699709946f0983b6899f0c7428785f9c596dea8ac150619f20`.
That private archive uses the supplied original resources and removes both018
PNG overrides. All other artwork remains the same Cartoon island/pose assets.

One smoke precedes one full74-display Rear waypoint route. The first mirrored
arrival is display063 at4920ms, actual draw `[1,394,209,18]`, HD origin `(788,418)`.
Native API/path/draw/flip/time events match the revised capture exactly. All
non018 displays and all pixels outside018's64x154 canvas remain identical.

The original smaller foot visually joins its gray contact shadow at the shore.
The revised smaller screen-left foot remains visibly higher. Excluding the
original gray shadow, the supplied reference foot ends at runtime row143 versus
revised row138, a5HD-pixel difference, scene rows561 versus556. These are
screen-space region observations, not anatomical left/right labels. The original
shadow is an additional contact cue, so the comparison does not attribute the
entire visual difference to leg length alone.

The original capture uses the port's diagnostic palette. It is not a DOSBox
capture or proof of original-executable color/compositing/timing parity. No exact
numerical sand-boundary gap is asserted. Same-camera nearest4 crops and both
actual arrival PNGs are preserved in `evidence-v1`.

`capture.py` executes this one smoke/full pair in Xvfb with source mounted
read-only; it refuses existing scratch output. `preserve.py` is a one-time writer
for historical evidence and must not be rerun into the frozen folder. No new
browser, generation, runtime asset promotion or production change is involved.
